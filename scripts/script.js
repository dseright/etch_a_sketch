$(document).ready(function() {
	function grid(input) {
		// saved indent to check if input will crash/lag browser
			for(i = 0, i < input, i++){
				for(j = 0, j < input, j++){
					//code to add a single div which will iterate to input
				}
			}
	}
})